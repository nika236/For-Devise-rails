<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <a class="navbar-brand" href='<%= root_path %>'>Alpha Blog </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" href='#'>Bloggers </a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="<%= articles_path %>">Articles </a>
        </li>
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to 'Log out', destroy_user_session_path,
                        class: "nav-link active",
                        data: { turbo_method: :delete,
                                turbo_confirm: "Are you sure?" } %>
          </li>
          <li class="nav-item">
            <a class="nav-link active" href="<%= edit_user_registration_path %>">Edit</a>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to 'Log in', new_user_session_path,
                        class: "nav-link active" %>
          </li>
          <li class="nav-item">
            <%= link_to 'Sign up', new_user_registration_path,
                        class: "nav-link active"%>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>